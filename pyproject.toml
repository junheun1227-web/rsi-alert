[project]
name = "rsi-alert-kakao-skill"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["yfinance>=0.2.40", "pandas>=2.0", "requests>=2.31"]

[tool.vercel]
entrypoint = "api.kakao_skill:handler"
